import CreateAccount from "./CreateAccount";
import Login from "./Login";

function ManageAccount({user, setUser, users, setUsers}) {
    return (
        <div>
            <h1>ManageAccount</h1>
            {/* <CreateAccount user={user} setUsers={setUsers} setUser={setUser}/> */}
        </div>
    )
}

export default ManageAccount;